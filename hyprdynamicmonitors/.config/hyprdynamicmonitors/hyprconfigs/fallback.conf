# ==============================================================================
# Fallback Profile - Generic Configuration
# ==============================================================================
# Active when: No other profile matches
# Use case: Unknown monitor configuration, safe default
#
# This ensures Hyprland always has a valid monitor config

# Configure all connected monitors with preferred settings
# Hyprland will auto-position them
monitor=,preferred,auto,1
