# Papercuts

Small frictions hit while working here — dead-end tool calls, broken links,
confusing setup steps, flaky commands, misleading errors, non-obvious gotchas.
Not blocking; logged so this repo can be sanded down. Distinct from tracked
bugs and from a work log.

Append with `papercut "<what got in the way>"`. Check off (`- [x]`) or delete
entries as they're fixed.

## Open

- [ ] 2026-07-11 — Debugging Neovim with temporary -u init files was easy to botch because mktemp paths without .lua are treated as Vimscript, producing misleading E480 errors from Lua code.
- [ ] 2026-07-11 — Editing an Obsidian Excalidraw canvas via 'obsidian eval': relied on app.workspace.activeLeaf.view, which silently pointed at a DIFFERENT open Excalidraw pane (a second .md), so all my probes/styles/test-writes ran against the wrong drawing and produced a baffling 112-vs-228 element-count discrepancy. Fix: resolve the target leaf by file.path (getLeavesOfType('excalidraw').find(l=>l.view.file.path===...)), never trust activeLeaf. · _opus-4.8_
- [ ] 2026-07-11 — ExcalidrawAutomate: addElementsToView() and deleteViewElements() require ea.setView(view) first. Without it they no-op — but the eval still returns success-looking data (before==mid==after), masking that nothing happened. The failure only showed up as a [error] log line I nearly missed. · _opus-4.8_
- [ ] 2026-07-11 — ExcalidrawAutomate ea.deleteViewElements(ids) did not actually delete (element count unchanged). Had to fall back to api.updateScene({elements: all.map(e=> kill.has(e.id)?{...e,isDeleted:true}:e)}) + await view.save(). That updateScene+isDeleted path is the reliable delete/undo. · _opus-4.8_
- [ ] 2026-07-11 — 'obsidian eval' does not support top-level await; a script with bare 'await' fails with no clear message (surfaces downstream as a JSON parse error). Must wrap the whole body in an async IIFE and 'return JSON.stringify(...)'. · _opus-4.8_
- [ ] 2026-07-11 — obsidian 'dev:screenshot' is viewport-only — no export-selected-elements. To capture a specific diagram region I had to setActiveLeaf + excalidrawAPI.scrollToContent(els,{fitToContent:true}) + force zoom, then screenshot. A one-shot 'export these element ids to png' would remove a lot of boilerplate. · _opus-4.8_
- [ ] 2026-07-12 — Two names for the same Obsidian CLI: /opt/homebrew/bin/obsidian (app-installed symlink) and obsidian-cli via .zprofile PATH append. Permission allow-rules in vault settings end up split across both spellings, so approvals don't accumulate. Pick one canonical name (obsidian) and drop the .zprofile PATH line. · _fable-5_
- [ ] 2026-07-12 — Editing claude/.claude/settings.json from a bg job dead-ended: worktree.baseRef=fresh branches from origin/main, but that file exists only on the mac branch (absent from main), so the isolation worktree lacked the file entirely — while the bg-edit guard blocked the shared checkout. Had to manually 'git worktree add -b … mac' and EnterWorktree by path. For mac-only files the fresh-from-main isolation is unusable. · _opus-4.8_
- [ ] 2026-07-12 — Repo-root .claude/settings.json is gitignored, so project-scoped Claude settings for the dotfiles repo itself can't be version-controlled. Had to write a dotfiles-drift skill override as an untracked local file instead of committing it. · _opus-4.8_
- [ ] 2026-07-16 — ctx_batch_execute's shell wrapper rejected a valid for-loop pipeline with 'parse error near for'; rerunning the audit as a direct rg command worked.
- [ ] 2026-07-16 — Pi's published package.json advertises a tsgo build script, but the installed production package contains no TypeScript compiler binary; extension type-check verification needs a separate compiler path.
- [ ] 2026-07-16 — GNU Stow 2.4.1 does not accept the intuitive --dry-run option; its simulation flag is -n/--no/--simulate.
- [ ] 2026-07-16 — macOS mktemp requires its X template at the end; '/tmp/name.XXXXXX.json' yielded an empty path and made 'tsc -p' report a misleading missing argument.
- [ ] 2026-07-16 — context-mode ctx_execute_file blocks Pi documentation and installed package files outside the workspace, despite those paths being required by the Pi task instructions; had to fall back to direct read.
- [ ] 2026-07-16 — context-mode could not read an invoked skill outside the project root, requiring a fallback to the read tool despite context-mode routing guidance
- [ ] 2026-07-16 — Direct web_search was unavailable because BRAVE_SEARCH_API_KEY is unset, even though delegated web-search-researcher agents can access current docs.
- [ ] 2026-07-16 — ctx_execute shell wrapper runs zsh semantics: assigning to status failed because status is readonly, after the expensive git clone had already completed.
- [ ] 2026-07-16 — context-mode ctx_execute_file cannot read Pi docs outside the project root, despite project instructions requiring those files; had to fall back to read.
- [ ] 2026-07-16 — PTY popup test sent input before Bash reached read, leaving bytes in canonical buffering and causing a false timeout; wait for the rendered prompt before writing input.
- [ ] 2026-07-16 — ctx_execute_file cannot read Pi's globally installed docs/examples outside the project root, despite those docs being required for Pi extension work; had to fall back to read.
- [ ] 2026-07-16 — launchctl bootstrap returned Input/output error after replacing the Headroom LaunchAgent plist with a Stow symlink; service state needed manual diagnosis.
- [ ] 2026-07-16 — Headroom 0.30 eagerly probes macOS Keychain fallback credentials even when GITHUB_COPILOT_GITHUB_TOKEN is explicitly set, causing an unexpected login-password dialog when the launchd proxy restarts.
- [ ] 2026-07-16 — ctx_batch_execute ran a valid-looking shell loop under a shell that reported 'parse error near for'; had to rerun the filesystem check separately.
- [ ] 2026-07-16 — Web search for current Headroom release failed because BRAVE_SEARCH_API_KEY is not configured; had to rely on local installed integration and GitHub alternatives.
- [ ] 2026-07-16 — A reachable git tree referenced a missing blob during local secret-history scanning; scanner needed to tolerate partial/missing objects.
- [ ] 2026-07-16 — ctx_execute_file blocked reading ~/.headroom launchd logs outside the project root during local service verification; had to fall back to a shell filter.
- [ ] 2026-07-16 — A ctx_execute source search failed because nested shell quoting around a ripgrep pattern was brittle; use an argument array or a simpler fixed pattern.
- [ ] 2026-07-16 — context-mode cannot read required Pi docs outside the project root; had to retry with direct read.
- [ ] 2026-07-16 — context-mode could not read globally installed skill files outside the project root; had to fall back to direct read.
- [ ] 2026-07-16 — context-mode cannot read Pi docs under /opt even though the project instructions require them; had to fall back to the read tool.
- [ ] 2026-07-16 — ctx_execute_file cannot read required skill/docs outside the project root, forcing fallback to read despite context-mode guidance.
- [ ] 2026-07-16 — settings.json changed between read and edit, so the exact replacement unexpectedly failed.
- [ ] 2026-07-16 — The requested apply_patch helper is unavailable in PATH; the edit tool also could not match text that read displayed verbatim, forcing a full-file write.
- [ ] 2026-07-16 — Pi RPC extension load check with --no-session still persisted the selected model into settings.json; use an ephemeral PI_CODING_AGENT_DIR for validation.
- [ ] 2026-07-17 — ctx_execute lost Neovim VIMRUNTIME output because print was not captured, producing a misleading /doc/cmdline.txt ENOENT during diagnosis.
- [ ] 2026-07-18 — Pi MCP config audit needed current GitHub MCP docs, but web_search was unavailable because BRAVE_SEARCH_API_KEY is not configured; required a fallback or user setup.
- [ ] 2026-07-18 — Todo state disappeared after a foreground Explore agent run; tasks #4-#6 were no longer present when updating them.
- [ ] 2026-07-18 — test_glab_board.py (untracked, lives in ~/.claude/skills/forge/ beside the symlinks) asserts glab mr create gets --related-issue, but glab-board never passed that flag in its entire git history — the test encodes a feature that was never shipped, so the suite fails at HEAD before any change · _fable-5_
- [ ] 2026-07-18 — bare python3 on this machine resolves to ~/.local/bin/python3 which has no pytest — running the forge skill tests requires 'uv run --with pytest pytest'; nothing documents this and the delegate found a different interpreter than the driver did · _fable-5_
- [ ] 2026-07-18 — context-mode ctx_execute_file rejected a known ~/.claude skill path outside the project root, forcing a second inspection route for a simple cross-harness skill audit.
- [ ] 2026-07-18 — stow --restow --no-folding agents aborted because a runtime forge/__pycache__ file exists in both the package and target; unrelated runtime state blocks stowing a newly added shared skill.
- [x] 2026-07-18 — tldraw skill installer splits one skill across ~/skills/tldraw-offline plus separate regular copies under Claude, Cursor, and Codex, while omitting ~/.agents/Pi; canonical ownership and update direction are unclear and copies can drift. Fixed locally: the tracked `.agents` bundle now owns every skill/wrapper path, installer markers were removed, the sync manifest was retired, and Pi has live-context injection.
- [x] 2026-07-18 — tldraw SKILL.md calls /Users/stanwang/skills/tldraw-offline/tq instead of a skill-relative helper, while tq and inject-server-context.sh embed an absolute /Users/stanwang path; relocating the skill into a shared dotfiles hub silently leaves runtime dependencies behind. Fixed: helpers, skill text, wrappers, and hook commands now derive paths from `$HOME` and the shared `.agents` hub.
- [x] 2026-07-18 — tldraw /api/doc/:id/script-workspace rejects a percent-encoded document id (tldr%3A...) as not found even though path parameters are normally encoded; clients must know to send the raw colon-bearing id. Mitigated locally: `tq` normalizes encoded colons before calling the signed upstream app.
- [x] 2026-07-18 — tldraw document-script updates are watched file-by-file with no atomic multi-file apply; creating config.js, its imported custom-shape module, data modules, and main.js can cause transient failed applies or repeated editor remounts. Mitigated locally: `tq-sync` uses rsync delayed updates inside the app's 150 ms debounce and waits for `script-status.state=applied`.
- [x] 2026-07-18 — tldraw api.imports is described like a symbol list but is a large module-descriptor/export index; a naive includes(name) check always fails and printing it accidentally emits tens of thousands of lines. Fixed in shared guidance with the descriptor shape and a narrow symbol-query example.
- [ ] 2026-07-18 — ctx_batch_execute mangled a quoted '/Applications/tldraw offline.app' multi-command batch and produced a zsh parse error near for; rerun the source search as isolated commands when app paths contain spaces.
- [x] 2026-07-18 — tldraw api.imports module descriptors do not all carry an exports array; the first narrow-query guidance used exports.filter directly and failed live. Fixed in shared guidance with `(exports ?? [])` and verified against the live API.
- [ ] 2026-07-18 — background research agent replaced the parent session's todo list with its own four tasks, deleting the driver's in-progress research/verification/synthesis tasks; todo state is unexpectedly shared across subagents.
- [ ] 2026-07-18 — context-mode ctx_execute_file rejects installed Pi docs and git-package sources outside the project root, forcing a fallback reader even when those paths are locally readable. · _gpt-5.4_
- [ ] 2026-07-18 — Declared a background pi worker dead from ps greps matching 'pi' in argv — pi runs as bare 'node', so every grep missed it and rtk's compaction of empty ps output hid the ambiguity; killed a possibly-live worker. Liveness-check pi workers with pgrep -P on the wrapper pid, not argv greps. · _fable-5_
- [ ] 2026-07-18 — tldraw maps Shift+1 (the `!` character on this keyboard) to Zoom to Fit, so typing `!` also changes the camera. Container bubbling, document capture, and event-scoped `zoomToFit` guards passed synthetic tests but all failed physical-key tests; all local workarounds were reverted. This needs a host-level action/keymap override or upstream fix.
- [ ] 2026-07-18 — context-mode blocked analysis of explicitly user-provided files outside the cwd project root, requiring a fallback to direct reads despite the context-mode skill mandate.
- [ ] 2026-07-18 — pi -p reviewer at xhigh fanned out its own subagents, then ended with 'results were incorporated into the Round 2 report' without printing the report; with --no-session the deliverable evaporated entirely. Handoffs must pin delivery: final stdout message IS the report, plus a required write to an explicit report file. · _gpt-5.6-sol_
- [ ] 2026-07-18 — context-mode could not read a required skill file outside the project root; had to fall back to read
- [ ] 2026-07-18 — context-mode ctx_execute_file cannot read an explicitly invoked skill outside the current project root, requiring fallback to read despite the active context-mode rule.
- [ ] 2026-07-19 — ctx_execute_file blocks documented reads outside the project root, even for configured Pi docs; had to fall back to read/index tooling.
- [ ] 2026-07-19 — Restowing the agents package aborts because generated forge/__pycache__ files exist in both the stow source and target; rerun requires an explicit __pycache__ ignore.
- [ ] 2026-07-19 — context-mode shell execution uses zsh, where `status` is read-only; generic shell snippets capturing `$?` into `status` fail after running the command.
- [ ] 2026-07-19 — A script run through a GNU Stow file symlink cannot derive the repository root from dirname(BASH_SOURCE); resolve the source file with realpath before walking parent directories.
- [ ] 2026-07-19 — ctx_execute_file cannot inspect the user-referenced Headroom auth doc outside the project root, so the task had to fall back to read.
- [ ] 2026-07-19 — ctx_execute_file could not read required skill files outside the current project root, so the mandated context-mode-first workflow had to fall back to direct read. · _gpt-5.6-sol_
- [ ] 2026-07-24 — Diagnosing pi extension deps: ~/.config/pi/agent/npm/node_modules/@earendil-works/ exists but is empty — dead end when locating pi's real packages, which live under /opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent (with pi-tui nested inside). The empty dir probably deserves cleanup. · _fable-5_
- [ ] 2026-07-24 — Pi vim-editor: goDocumentStart Up-spammed to reach the top, but pi-tui's editor turns Up on the first visual line (col 0 / empty / browsing) into prompt-history navigation — any moveToOffset silently replaced the buffer with a history entry once one prompt had been submitted. Only surfaced in live TUI testing because unit harnesses and fresh sessions have empty history. Fixed by anchoring at the end (setText trick) and walking Left, plus clamping j/k. · _fable-5_
- [ ] 2026-07-27 — Superwhisper rewrote its live mode JSON after creation, so an exact text edit based on the original content no longer matched.
- [ ] 2026-07-28 — Adding a Raycast script-command directory has no CLI path — it's GUI-only (Settings → Extensions → + → Add Script Directory). Meant a dotfiles-tracked script bundle can't be fully provisioned by bootstrap.sh; the user has to click through once per machine. Raycast's prefs plist stores it as an opaque bookmark blob (NSOSPLastRootDirectory), so scripting it is not safe either.
- [ ] 2026-07-28 — Read empty `find` output as proof three directories were deleted, when rtk had simply compacted the result away — reported them gone to the user, then found all 16 files intact on a later explicit `[ -e ]` check. Same class as the 2026-07-18 'ps grep + rtk empty output' entry. Absence of output is never evidence of absence: verify deletion with an explicit per-path test that prints on both branches, not with a listing command that prints nothing when it finds nothing.
- [ ] 2026-07-29 — ctx_execute_file cannot read configured skill files outside the current project root, requiring a fallback read call.

### Moved from madden-agent 2026-08-04 — personal tooling/config (context-mode, shell, pi/Copilot, tldraw, misc) and swarm/delegation patterns

- [ ] 2026-07-12 — git push/ls-remote to gitlab.ea.com hangs silently (no error) when VPN is down, and perl-alarm wrappers don't kill git's spawned git-remote-https child — pipeline keeps waiting past the alarm. Probe connectivity with a hard-killed curl before any git network op. · _fable-5_
- [ ] 2026-07-12 — pi -p backgrounded (&) blocks on stdin even with a correct positional prompt arg via "$(cat file)" — sits at 0% CPU, 0 bytes output, no error, looks identical to an auth failure. Fix: redirect '< /dev/null'. Distinct from the DELEGATION.md nested-quote-mangling case; this is single-shell correct quoting but inherited-open-stdin on a background job. · _claude-opus-4.8_
- [ ] 2026-07-12 — Launching pi from a script needs the binary path /opt/homebrew/bin/pi — bare 'pi' resolves to a shell function wrapper (PI_INTERNAL_SCROLLBACK=1 command pi) that isn't on PATH the same way in a non-interactive shell. macOS has no 'timeout' so wrap with perl -e 'alarm N; exec @ARGV'. · _claude-opus-4.8_
- [ ] 2026-07-13 — Subagent reading transcript JSONL via inline cat/jq got word-dropped text (reported phantom 'degraded agent text' bug); same terminal-wrapper mangling as pytest — redirect to a file and Read it, or parse with python writing to a file. · _fable-5_
- [ ] 2026-07-16 — context-mode shell runs under zsh, where status is read-only; a redirected pytest wrapper using status=$? failed after the test run. Use rc instead.
- [ ] 2026-07-16 — context-mode shell runs under zsh, where assigning to status fails because status is read-only; test wrapper must use rc instead.
- [ ] 2026-07-17 — context-mode ctx_execute_file blocks /tmp even when ctx_execute created the file; required copying into the worktree for analysis.
- [ ] 2026-07-17 — ctx_execute ignores cwd for Python scripts, so importing project modules fails unless the repo path is manually added to sys.path; this is easy to miss while validating live state.
- [ ] 2026-07-17 — Batch-indexing the full rendered transcript let repeated model-input sections dominate S4 query results and hid the latest user-visible tail; a targeted raw-event parser was still required.
- [ ] 2026-07-18 — ctx_execute rendered a JavaScript source preamble plus unlabeled compressed values, making its otherwise concise documentation inventory difficult to parse.
- [ ] 2026-07-18 — Background-agent outputs are written outside the project root, but context-mode blocks ctx_execute_file there; extracting a large agent report requires loading it through the regular read tool.
- [ ] 2026-07-18 — A background subagent's todo IDs and status updates mutated the parent session task list, replacing the documentation-audit task with its internal checklist; subagent todo state should be isolated.
- [ ] 2026-07-18 — Documentation freshness inventory aborted halfway because the audit script treated rg's normal no-match exit code as fatal; audit helpers should tolerate no inbound references.
- [ ] 2026-07-18 — ctx_batch_execute's shell wrapper parsed a command beginning with a for-loop incorrectly, so the documentation reference scan had to be rerun separately.
- [ ] 2026-07-18 — The detached old-doc locator failed before starting because its default github-copilot provider had no API key, despite the parent session having working models.
- [ ] 2026-07-18 — todo task IDs/state reset during the antiquated-test background audit: previously created task #14 disappeared and reappeared as #1, while earlier tasks vanished.
- [ ] 2026-07-18 — Canonical pytest wrapper used shell variable name 'status', which is read-only under the ctx_execute zsh shell; use 'rc' instead.
- [ ] 2026-07-18 — Subagents dispatched into pre-made worktrees under .claude/worktrees/ get their Edit/Write tool calls rejected by the shared-checkout isolation guard (and EnterWorktree refuses too since their cwd isn't the worktree) — workers fall back to editing via Bash. Guard seems to not recognize driver-created worktrees as isolated for subagents. · _fable-5_
- [ ] 2026-07-18 — pi (github-copilot provider) stalled on all review-loop invocations from a subagent during dock Phase 2 — two consecutive dead launches with monitors armed on nothing; same auth-race/stall class as earlier parallel launches. Review loops delegated to pi need a hard timeout + native fallback baked into the first instruction, not discovered per-run. · _fable-5_
- [ ] 2026-07-18 — Saving the live tldraw board requires Cmd+S, but macOS denied osascript keystrokes; the local canvas API exposes edits but no documented save endpoint.
- [ ] 2026-07-18 — The first tldraw screenshot targeted api.getFocusedDoc(), which was a different open file than the document passed to /exec; use the known document id for screenshots in multi-window sessions.
- [ ] 2026-07-18 — ctx_execute_file is confined to the repo and cannot inspect the live tldraw script workspace under Application Support; had to fall back to direct file tools after the documented tldraw workflow.
- [ ] 2026-07-19 — codebase-analyzer dispatch failed with an opaque invalid_request_body API error · _headroom-copilot/claude-opus-4.8_
- [ ] 2026-07-19 — tldraw save confirmation can be misleading: the user reported Cmd+S succeeded, but api.getDocs still showed unsavedChanges:true and the .tldraw archive lacked the new script.
- [ ] 2026-07-19 — The bundled Hammerspoon hs CLI hangs when hs.ipc is not loaded; add require("hs.ipc") before using it for setup verification.
- [ ] 2026-07-19 — context-mode ctx_execute_file cannot read required skill files outside project root; had to retry with read.
- [ ] 2026-07-19 — context-mode ctx_execute_file cannot read required skill files outside the project root; had to fall back to read.
- [ ] 2026-07-20 — tldraw-offline examples imply api.getPages exists, but live server reports api.getPages is not a function; had to use editor.getPages via /exec.
- [ ] 2026-07-21 — Provided tldraw live script workspace path wd-21968-2 did not exist; had to rediscover the active workspace path before staging.
- [ ] 2026-07-22 — context-mode ctx_execute_file blocks reading a sibling repo even when the user explicitly asks to inspect it; had to fall back to direct read.
- [ ] 2026-07-22 — api.getScreenshot with mode:'canvas' + bounds returns captureMode:'window' with bounds:null on tldraw Desktop 1.11 — bounded region capture silently ignored; workaround: editor.zoomToBounds via /exec then window screenshot · _fable-5_
- [ ] 2026-07-23 — context-mode blocked reading an explicitly listed skill outside the project root, so the skill had to be loaded with the direct read tool.
- [ ] 2026-07-23 — tq-sync usage documents '--delete' but not its argument position; passing it first made the document id parse as the source directory.
- [ ] 2026-07-23 — Rapid scripted page switching plus window screenshots caused the tldraw Desktop UI to show a refresh/reset error even though the local canvas API remained responsive.
- [ ] 2026-07-23 — A reset verification command used set -e with rg; the expected no-match exit status aborted the check before printing results.
- [ ] 2026-07-24 — context-mode ctx_execute_file cannot read required skill docs outside the project root, forcing a direct read fallback.
- [ ] 2026-07-30 — ctx_index include globs unexpectedly indexed only 1 Citadel file and 0 Webstore files even though matching Java/proto files exist under the supplied roots; the include semantics were not obvious.
- [ ] 2026-08-05 — dotfiles-drift audit flags tap-qualified brew declarations (modem-dev/tap/hunk) as missing even when installed — it compares against short formula names; declaring the short name with a tap line works around it · _fable-5_
- [ ] 2026-08-05 — DELEGATION.md launcher watchdog ('~0 CPU after 90s = stalled, kill') killed two healthy pi workers mid-run — pi idles near 0 CPU while awaiting model responses; progress signals (git log, output growth, network) must be checked before killing · _fable-5_
- [ ] 2026-08-06 — Skill post-check used system Python with PyYAML and failed because dotfiles has no PyYAML environment; the shell command also lacked set -e, so the subsequent commit still ran. Use the skill installer/verifier or dependency-free frontmatter checks.
- [ ] 2026-08-08 — ls is aliased to eza, so scripted 'ls -t' (sort by mtime) fails with an eza option error — agents probing file ages must use /bin/ls or stat · _claude-fable-5_
- [ ] 2026-08-08 — global CLAUDE.md says 'read ~/.claude/DELEGATION.md when delegating' but the symlink is missing — DELEGATION.md exists in claude/.claude/ yet was never stowed; needs 'stow --restow --no-folding claude' · _claude-fable-5_
- [ ] 2026-08-08 — DELEGATION.md pattern-A launch failed against current pi: 'Unknown option: --approve' and 'Unknown provider headroom-copilot' — doc flags/provider list are stale vs installed pi ; needs re-validation · _claude-fable-5_
- [ ] 2026-08-10 — install-skill orchestrate hard-aborts at the claude-consumer check (~/.claude/skills/orchestrate is the deliberate Claude-adapted shadow, a real dir) and never reaches the pi consumer step — had to hand-create the pi symlink. Script could recognize an intentional claude-variant shadow or keep going to remaining consumers. · _fable-5_
- [ ] 2026-08-10 — Work→personal migration left several brew binaries linked against since-removed libs: node→libllhttp.9.3, tmux-fingers→bdw-gc/libgc.1 (plugin then silently binds nothing and prefix+, falls back to rename-window), plus libtiff/webp stale keg-tab cycle. Symptom is always a dyld 'Library not loaded' crash; fix is brew install/upgrade of the affected formula. A post-migration 'brew doctor + run each brew-installed binary once' sweep would catch these. · _fable-5_
- [ ] 2026-08-10 — Handoff session inherited a dirty index: lazy-lock.json and optimize-media were pre-staged, so a lazygit-only commit swept them in and had to be rewritten+force-pushed. Check for staged files before committing in a resumed session. · _fable-5_
- [ ] 2026-08-10 — Handoff flagged 'karabiner_grabber not running → needs GUI grants', but Karabiner ≥15.7 renamed that process to Karabiner-Core-Service — the check was against an obsolete process name and the stack was actually healthy. Health-check by Core-Service + dext presence, not grabber. · _fable-5_
- [ ] 2026-08-10 — Fujifilm X-T30 III RAF files show no Finder/Quick Look preview on macOS 15 Sequoia. qlmanage -t just hangs past 120s with no error, and mdls reports kMDItemPixelCount=0 — nothing says 'unsupported camera', so it reads like a broken Finder setting. Real cause: Apple only added the X-T30 III RAW profile in macOS 26 Tahoe and does not backport camera support. Workaround is scripts/bin/raf2dng (Adobe DNG Converter). · _claude-opus-5_
- [ ] 2026-08-10 — Claude Code TUI runs in tmux alternate screen (alternate_on=1, history_size≈0): capture-pane -S - and tmux-fingers only ever see the visible viewport of a Claude pane — scrolled-away content lives in Claude's internal scrollback, not tmux's. Affects bind H history viewer and any screen-parsing feature. · _fable-5_
- [ ] 2026-08-10 — glab-board setup --board dies midway when the gh token lacks the 'project' scope — it could pre-check scopes (gh auth status) and print the 'gh auth refresh -s project' fix before attempting createProjectV2 · _fable-5_
- [ ] 2026-08-10 — raf2jpg silently skipped every RAF that had a companion camera .JPG: macOS is case-insensitive, so the target 'X.jpg' collided with the camera's 'X.JPG' and the skip-if-exists check read it as already-converted. Reported success while leaving the film-sim file in place. Fixed by writing X_neutral.jpg. Any Mac script deriving an output name by swapping extension needs to assume case-insensitive collisions. · _claude-opus-5_
- [ ] 2026-08-10 — rtk's find wrapper rejects compound predicates/actions (-exec, -not) with 'use find directly' — fine, but it means a working find command fails only after you've written it. Worth knowing before reaching for -exec. · _claude-opus-5_
- [ ] 2026-08-10 — glab-board view <n> prints nothing on GitHub-hosted repos (pi-talk) — no output, no error, exit 0. Had to fall back to 'gh issue view <n>' to read the ticket contract. Likely the view path only implements the GitLab API branch; it should either shell out to gh or say 'unsupported forge'. · _opus-5_
- [ ] 2026-08-10 — glab-board start <n> creates a fresh worktree with no node_modules, so 'npm test' fails until you remember to npm install there first. Worth either running the install as part of start, or printing a one-line 'run npm install' hint when the repo has a package.json and no node_modules. · _opus-5_
- [ ] 2026-08-10 — Splitting one worktree's work into two per-ticket commits is awkward when both tickets edit the same files (pi-talk #28/#29 both touched src/speaker.ts + test/speaker.test.ts). No non-interactive way to stage partial hunks, so I had to copy the combined files to a scratchpad, 'git checkout --' them, re-apply just the first ticket's edits, commit, then restore. A 'glab-board' helper or a documented recipe for this would save the dance. · _opus-5_
- [ ] 2026-08-10 — Reading a GitHub issue body through the rtk hook silently mangles it — 'gag: remove own session flag' collapsed into word salad ('--all`: per-session on/off, on/off, 7 JSON JSON 0'), and even 'rtk proxy gh issue view' still compacted the tail. For closed execution contracts the only reliable read is redirecting to a file and using Read. Worth a hook exclusion for 'gh issue view'/'gh pr view'. · _opus-5_
- [ ] 2026-08-10 — tmux run-shell does NOT inherit the focused pane's process environment, so any 'bind ... run-shell' that needs CLAUDE_CODE_SESSION_ID gets nothing. Easy to get wrong because 'tmux new-session -e VAR=x' DOES survive into run-shell — testing that way gives a false positive. Verify with a var exported only inside the pane's process. · _opus-5_
- [ ] 2026-08-10 — cc-talk 'talk' collides with BSD /usr/bin/talk under any PATH that orders /usr/bin first (tmux server env did) — keybind silently ran the 1983 chat tool; consider whether the executable deserves a collision-free name · _fable-5_
- [ ] 2026-08-10 — Writing `speed_confirm "speed $new×"` in scripts/bin/talk silently broke under set -u: bash/zsh treat the multibyte × as part of the variable name, so it looked up `new×` and died with 'unbound variable' AFTER the file write had already happened. Only caught it because I diffed expected output. Any '$var' immediately followed by a non-ASCII glyph needs ${var} braces. · _opus-5_
- [ ] 2026-08-10 — Verifying a ghostty config change had no obvious command: `ghostty` is not on PATH (GUI app), so `ghostty +validate-config` fails with 'command not found' and rc=0 through a pipe, which reads as a pass. The real binary is /Applications/Ghostty.app/Contents/MacOS/ghostty. Worth a PATH shim or a note in the ghostty package. · _opus-5_
- [ ] 2026-08-10 — gh issue view 35 through the rtk hook returned a mangled body — whole clauses dropped and merged, making a 'closed execution contract' unreadable as a contract. Had to rerun with 'rtk proxy gh issue view --json body' to get exact bytes. Issue/MR bodies read as specs should bypass rtk compaction by default. · _opus-5_
- [ ] 2026-08-10 — Fake-HOME shell tests on macOS: 'env -u VAR' is GNU-only, BSD env rejects it with 'env: -u: No such file or directory' (rc 127) and the failure looks like a missing binary rather than a bad flag. Use a subshell with 'unset VAR' in dotfiles test scripts. · _opus-5_
- [ ] 2026-08-10 — Fake-HOME shell tests: macOS BSD 'env' rejects options after variable assignments — 'env HOME=x -u TMUX cmd' fails with 'env: -u: No such file or directory' instead of unsetting. GNU env accepts either order. Put -u/-i before all NAME=VALUE pairs when writing stub-env tests. · _opus-5_
- [ ] 2026-08-10 — glab-board grab failed in the pi-talk clone with 'GitHub canonical project unset; run glab-board setup --board first', even though the repo had exactly one linked Projects v2 board. Had to hand-pin 'git config forge.githubProjectId PVT_...'. gh_project_id() could auto-pin when the repo has exactly one linked project instead of failing closed. · _opus-5_
- [ ] 2026-08-10 — Reading claude/.claude/settings.json with the Read tool returned garbled content — roughly lines 40-131 came back as bare fragments and stray numbers with the JSON structure dropped, so the hooks object was unreadable. Had to reconstruct it from a targeted grep. Looks like output compaction being applied to a file read where it should not be. · _opus-5_
- [ ] 2026-08-10 — 'timeout' is not on PATH on this macOS box (no coreutils gtimeout shim), so the reflexive 'timeout 180 claude -p ...' died with 'command not found' before the probe ran. A gtimeout->timeout shim in scripts/bin would save the retry. · _opus-5_
- [ ] 2026-08-10 — cc-talk #41 worker registered MessageDisplay/UserPromptSubmit hooks in live settings.json a minute before restowing the script symlink — every session threw 'No such file or directory' on prompt submit during the gap. Hook rollout order must be: write script, restow, THEN register in settings · _fable-5_
- [ ] 2026-08-11 — Expected ~/.config/karabiner/karabiner.json to be a stow symlink (karabiner is in the no-folding list); it's actually a HARDLINK to the repo file (same inode). Works for in-place edits, but any rename-style write (Karabiner GUI save, some editors) will silently decouple live config from the repo. Worth deciding: restow to symlink and accept GUI saves breaking it, or document the hardlink. · _fable-5_
